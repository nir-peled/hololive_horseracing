"use client";
import { RaceContestantsData } from "@/src/lib/types";

interface Props {
	id: bigint;
	contestants: RaceContestantsData;
}

// TODO: to be fileld later
export function RaceResultsEditForm({ id, contestants }: Props) {
	return <div></div>;
}
