import React from "react";
import { Locale } from "@/src/lib/types";

interface Props {
	locale: Locale;
}

export default async function UserBetsList({ locale }: Props) {
	return <div>UserBetsList</div>;
}
