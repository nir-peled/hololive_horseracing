const locales = ["en", "ja"];

const i18nConfig = {
	locales: locales,
	defaultLocale: "en",
	prefixDefault: true,
	localizationFolder: "@/locales",
};

export default i18nConfig;
export { locales };
